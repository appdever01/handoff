# Handoff frontend

Own only this surface. Read ../docs/PRD.md and ../docs/STATUS.md. Use codex/ branches for features. Never add source comments without request. Keep tests at two workers or fewer. Ask before browser automation. Keep checkout disabled until release gates pass. Backend owns shared API contracts in packages/contracts; frontend consumes the versioned package. Never publish secrets or original files.
