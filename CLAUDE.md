# Handoff workspace

The root orchestrates independent frontend, backend, and docs clones from the same remote. Never stage nested clones here. Feature branches use codex/. Read each surface's AGENTS.md before edits. No source comments unless requested. No browser automation without explicit user permission. Cap test workers at two. Never release original bytes on browser-reported payment success. Do not enable checkout until independent chain verification, finality and recovery pass release gates.
